# SMTP Configuration
# ------------------
# This is an example file. Copy this to a new file named .env and fill in your actual credentials.
# Do NOT commit the .env file to Git.

# The address of your SMTP server (e.g., 'smtp.gmail.com')
SMTP_HOST="smtp.example.com"

# Your full email address
SMTP_USERNAME="your-email@gmail.com"

# Your email password or App Password for Gmail/Outlook
SMTP_PASSWORD="your-app-password"

# The TCP port to connect to: 465 for SSL, or 587 for TLS
SMTP_PORT="465"

# The encryption method: 'ssl' or 'tls'
SMTP_SECURE="ssl"

# The "From" email address and name
SMTP_FROM_EMAIL="your-email@gmail.com"
SMTP_FROM_NAME="Army's Angels Integrated School"
